const axios=require('axios');function haversine(a,b,c,d){const R=6371,p=Math.PI/180,x=(c-a)*p,y=(d-b)*p,q=Math.sin(x/2)**2+Math.cos(a*p)*Math.cos(c*p)*Math.sin(y/2)**2;return R*2*Math.atan2(Math.sqrt(q),Math.sqrt(1-q))}async function getRoute(a,b,c,d){try{let u=`https://router.project-osrm.org/route/v1/driving/${b},${a};${d},${c}?overview=full&geometries=geojson`;let {data}=await axios.get(u,{timeout:5000}),r=data.routes?.[0];if(!r)throw Error();return{distanceKm:+(r.distance/1000).toFixed(2),etaMinutes:Math.round(r.duration/60),geometry:r.geometry,source:'OSRM_LIVE'}}catch(e){let km=haversine(a,b,c,d);return{distanceKm:+km.toFixed(2),etaMinutes:Math.max(3,Math.round(km/22*60)),geometry:{type:'LineString',coordinates:[[b,a],[d,c]]},source:'FALLBACK'}}}module.exports={getRoute,haversine};
